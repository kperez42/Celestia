# Celestia Cloud Functions - Environment Configuration Example
# Copy this file to .env for local development
# For production, use Firebase Functions Config: firebase functions:config:set

# =============================================================================
# Apple App Store Configuration
# =============================================================================
# Get your shared secret from App Store Connect:
# 1. Go to App Store Connect (https://appstoreconnect.apple.com)
# 2. Navigate to: Users and Access → Integrations → App-Specific Shared Secret
# 3. Generate if you don't have one
APPLE_SHARED_SECRET=your_app_specific_shared_secret_here

# =============================================================================
# Firebase Project Configuration
# =============================================================================
# Usually auto-configured by Firebase Admin SDK
# Only needed if you're running functions locally or in a custom environment
FIREBASE_PROJECT_ID=your-project-id

# =============================================================================
# External API Keys (Optional)
# =============================================================================

# Google Cloud Vision API (for image moderation)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_VISION_API_KEY=your_vision_api_key_here

# SendGrid (for email notifications)
# Get from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here

# =============================================================================
# Redis Configuration (Optional - if using Redis for rate limiting)
# =============================================================================
# By default, rate limiting uses Firestore
# Only configure if you're using Redis for better performance
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your_redis_password_if_needed

# =============================================================================
# Webhook Configuration (Optional)
# =============================================================================
# Slack webhook for admin alerts
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# For Local Development:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values
# 3. Run: firebase functions:config:get > .runtimeconfig.json
# 4. Start emulators: npm run serve
#
# For Production Deployment:
# Set environment variables using Firebase CLI:
#
#   firebase functions:config:set apple.shared_secret="YOUR_SECRET"
#   firebase functions:config:set sendgrid.api_key="YOUR_KEY"
#
# View current config:
#   firebase functions:config:get
#
# Deploy with config:
#   firebase deploy --only functions
#
# =============================================================================
